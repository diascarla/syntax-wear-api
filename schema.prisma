// Este arquivo é apenas um proxy para o schema real
// O schema real está em prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Importar models do schema real via referência (Prisma vai procurar em prisma/schema.prisma)
